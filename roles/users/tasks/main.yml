---
- name: Set up groups
  group: name={{ item.name }} state=present
  with_items: sys_groups

- name: Set up users
  user: name={{ item.name }} group={{ item.group }} state=present shell={{ item.shell | default('/bin/bash') }}
  with_items: sys_users
